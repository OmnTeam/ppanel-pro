// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/OmnTeam/npanel-pro/ent/proxydocument"
)

// ProxyDocument is the model entity for the ProxyDocument schema.
type ProxyDocument struct {
	config `json:"-"`
	// ID of the ent.
	// 文档ID
	ID int64 `json:"id,omitempty"`
	// 文档标题
	Title string `json:"title,omitempty"`
	// 文档内容
	Content string `json:"content,omitempty"`
	// 文档标签
	Tags string `json:"tags,omitempty"`
	// 显示
	Show bool `json:"show,omitempty"`
	// 创建时间
	CreatedAt time.Time `json:"created_at,omitempty"`
	// 更新时间
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProxyDocument) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case proxydocument.FieldShow:
			values[i] = new(sql.NullBool)
		case proxydocument.FieldID:
			values[i] = new(sql.NullInt64)
		case proxydocument.FieldTitle, proxydocument.FieldContent, proxydocument.FieldTags:
			values[i] = new(sql.NullString)
		case proxydocument.FieldCreatedAt, proxydocument.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProxyDocument fields.
func (_m *ProxyDocument) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case proxydocument.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case proxydocument.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case proxydocument.FieldContent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field content", values[i])
			} else if value.Valid {
				_m.Content = value.String
			}
		case proxydocument.FieldTags:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tags", values[i])
			} else if value.Valid {
				_m.Tags = value.String
			}
		case proxydocument.FieldShow:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field show", values[i])
			} else if value.Valid {
				_m.Show = value.Bool
			}
		case proxydocument.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case proxydocument.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProxyDocument.
// This includes values selected through modifiers, order, etc.
func (_m *ProxyDocument) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this ProxyDocument.
// Note that you need to call ProxyDocument.Unwrap() before calling this method if this ProxyDocument
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ProxyDocument) Update() *ProxyDocumentUpdateOne {
	return NewProxyDocumentClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ProxyDocument entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ProxyDocument) Unwrap() *ProxyDocument {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProxyDocument is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ProxyDocument) String() string {
	var builder strings.Builder
	builder.WriteString("ProxyDocument(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("content=")
	builder.WriteString(_m.Content)
	builder.WriteString(", ")
	builder.WriteString("tags=")
	builder.WriteString(_m.Tags)
	builder.WriteString(", ")
	builder.WriteString("show=")
	builder.WriteString(fmt.Sprintf("%v", _m.Show))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ProxyDocuments is a parsable slice of ProxyDocument.
type ProxyDocuments []*ProxyDocument
