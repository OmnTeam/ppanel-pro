// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/OmnTeam/ppanel-pro/ent/proxyannouncement"
)

// ProxyAnnouncement is the model entity for the ProxyAnnouncement schema.
type ProxyAnnouncement struct {
	config `json:"-"`
	// ID of the ent.
	// 公告ID
	ID int64 `json:"id,omitempty"`
	// 租户ID
	TenantID int64 `json:"tenant_id,omitempty"`
	// 公告标题
	Title string `json:"title,omitempty"`
	// 公告内容
	Content string `json:"content,omitempty"`
	// 是否显示
	Show bool `json:"show,omitempty"`
	// 是否置顶
	Pinned bool `json:"pinned,omitempty"`
	// 是否弹窗
	Popup bool `json:"popup,omitempty"`
	// 创建时间
	CreatedAt time.Time `json:"created_at,omitempty"`
	// 更新时间
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProxyAnnouncement) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case proxyannouncement.FieldShow, proxyannouncement.FieldPinned, proxyannouncement.FieldPopup:
			values[i] = new(sql.NullBool)
		case proxyannouncement.FieldID, proxyannouncement.FieldTenantID:
			values[i] = new(sql.NullInt64)
		case proxyannouncement.FieldTitle, proxyannouncement.FieldContent:
			values[i] = new(sql.NullString)
		case proxyannouncement.FieldCreatedAt, proxyannouncement.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProxyAnnouncement fields.
func (_m *ProxyAnnouncement) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case proxyannouncement.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case proxyannouncement.FieldTenantID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value.Valid {
				_m.TenantID = value.Int64
			}
		case proxyannouncement.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case proxyannouncement.FieldContent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field content", values[i])
			} else if value.Valid {
				_m.Content = value.String
			}
		case proxyannouncement.FieldShow:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field show", values[i])
			} else if value.Valid {
				_m.Show = value.Bool
			}
		case proxyannouncement.FieldPinned:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field pinned", values[i])
			} else if value.Valid {
				_m.Pinned = value.Bool
			}
		case proxyannouncement.FieldPopup:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field popup", values[i])
			} else if value.Valid {
				_m.Popup = value.Bool
			}
		case proxyannouncement.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case proxyannouncement.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProxyAnnouncement.
// This includes values selected through modifiers, order, etc.
func (_m *ProxyAnnouncement) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this ProxyAnnouncement.
// Note that you need to call ProxyAnnouncement.Unwrap() before calling this method if this ProxyAnnouncement
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ProxyAnnouncement) Update() *ProxyAnnouncementUpdateOne {
	return NewProxyAnnouncementClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ProxyAnnouncement entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ProxyAnnouncement) Unwrap() *ProxyAnnouncement {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProxyAnnouncement is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ProxyAnnouncement) String() string {
	var builder strings.Builder
	builder.WriteString("ProxyAnnouncement(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TenantID))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("content=")
	builder.WriteString(_m.Content)
	builder.WriteString(", ")
	builder.WriteString("show=")
	builder.WriteString(fmt.Sprintf("%v", _m.Show))
	builder.WriteString(", ")
	builder.WriteString("pinned=")
	builder.WriteString(fmt.Sprintf("%v", _m.Pinned))
	builder.WriteString(", ")
	builder.WriteString("popup=")
	builder.WriteString(fmt.Sprintf("%v", _m.Popup))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ProxyAnnouncements is a parsable slice of ProxyAnnouncement.
type ProxyAnnouncements []*ProxyAnnouncement
