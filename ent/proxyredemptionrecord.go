// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/OmnTeam/ppanel-pro/ent/proxyredemptioncode"
	"github.com/OmnTeam/ppanel-pro/ent/proxyredemptionrecord"
	"github.com/OmnTeam/ppanel-pro/ent/proxyuser"
)

// ProxyRedemptionRecord is the model entity for the ProxyRedemptionRecord schema.
type ProxyRedemptionRecord struct {
	config `json:"-"`
	// ID of the ent.
	// 兑换记录ID
	ID int64 `json:"id,omitempty"`
	// 兑换码ID
	RedemptionCodeID int64 `json:"redemption_code_id,omitempty"`
	// 用户ID
	UserID int64 `json:"user_id,omitempty"`
	// 订阅ID
	SubscribeID int64 `json:"subscribe_id,omitempty"`
	// 时间单位
	UnitTime string `json:"unit_time,omitempty"`
	// 数量
	Quantity int32 `json:"quantity,omitempty"`
	// 兑换时间
	RedeemedAt time.Time `json:"redeemed_at,omitempty"`
	// 创建时间
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProxyRedemptionRecordQuery when eager-loading is set.
	Edges        ProxyRedemptionRecordEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ProxyRedemptionRecordEdges holds the relations/edges for other nodes in the graph.
type ProxyRedemptionRecordEdges struct {
	// User holds the value of the user edge.
	User *ProxyUser `json:"user,omitempty"`
	// RedemptionCode holds the value of the redemption_code edge.
	RedemptionCode *ProxyRedemptionCode `json:"redemption_code,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProxyRedemptionRecordEdges) UserOrErr() (*ProxyUser, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: proxyuser.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// RedemptionCodeOrErr returns the RedemptionCode value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProxyRedemptionRecordEdges) RedemptionCodeOrErr() (*ProxyRedemptionCode, error) {
	if e.RedemptionCode != nil {
		return e.RedemptionCode, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: proxyredemptioncode.Label}
	}
	return nil, &NotLoadedError{edge: "redemption_code"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProxyRedemptionRecord) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case proxyredemptionrecord.FieldID, proxyredemptionrecord.FieldRedemptionCodeID, proxyredemptionrecord.FieldUserID, proxyredemptionrecord.FieldSubscribeID, proxyredemptionrecord.FieldQuantity:
			values[i] = new(sql.NullInt64)
		case proxyredemptionrecord.FieldUnitTime:
			values[i] = new(sql.NullString)
		case proxyredemptionrecord.FieldRedeemedAt, proxyredemptionrecord.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProxyRedemptionRecord fields.
func (_m *ProxyRedemptionRecord) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case proxyredemptionrecord.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case proxyredemptionrecord.FieldRedemptionCodeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field redemption_code_id", values[i])
			} else if value.Valid {
				_m.RedemptionCodeID = value.Int64
			}
		case proxyredemptionrecord.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.Int64
			}
		case proxyredemptionrecord.FieldSubscribeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field subscribe_id", values[i])
			} else if value.Valid {
				_m.SubscribeID = value.Int64
			}
		case proxyredemptionrecord.FieldUnitTime:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field unit_time", values[i])
			} else if value.Valid {
				_m.UnitTime = value.String
			}
		case proxyredemptionrecord.FieldQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				_m.Quantity = int32(value.Int64)
			}
		case proxyredemptionrecord.FieldRedeemedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field redeemed_at", values[i])
			} else if value.Valid {
				_m.RedeemedAt = value.Time
			}
		case proxyredemptionrecord.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProxyRedemptionRecord.
// This includes values selected through modifiers, order, etc.
func (_m *ProxyRedemptionRecord) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the ProxyRedemptionRecord entity.
func (_m *ProxyRedemptionRecord) QueryUser() *ProxyUserQuery {
	return NewProxyRedemptionRecordClient(_m.config).QueryUser(_m)
}

// QueryRedemptionCode queries the "redemption_code" edge of the ProxyRedemptionRecord entity.
func (_m *ProxyRedemptionRecord) QueryRedemptionCode() *ProxyRedemptionCodeQuery {
	return NewProxyRedemptionRecordClient(_m.config).QueryRedemptionCode(_m)
}

// Update returns a builder for updating this ProxyRedemptionRecord.
// Note that you need to call ProxyRedemptionRecord.Unwrap() before calling this method if this ProxyRedemptionRecord
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ProxyRedemptionRecord) Update() *ProxyRedemptionRecordUpdateOne {
	return NewProxyRedemptionRecordClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ProxyRedemptionRecord entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ProxyRedemptionRecord) Unwrap() *ProxyRedemptionRecord {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProxyRedemptionRecord is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ProxyRedemptionRecord) String() string {
	var builder strings.Builder
	builder.WriteString("ProxyRedemptionRecord(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("redemption_code_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RedemptionCodeID))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("subscribe_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SubscribeID))
	builder.WriteString(", ")
	builder.WriteString("unit_time=")
	builder.WriteString(_m.UnitTime)
	builder.WriteString(", ")
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", _m.Quantity))
	builder.WriteString(", ")
	builder.WriteString("redeemed_at=")
	builder.WriteString(_m.RedeemedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ProxyRedemptionRecords is a parsable slice of ProxyRedemptionRecord.
type ProxyRedemptionRecords []*ProxyRedemptionRecord
